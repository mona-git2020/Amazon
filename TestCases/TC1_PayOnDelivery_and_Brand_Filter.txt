*** Settings ***
Library		Selenium2Library	
Library		String
Library		Collections	
Resource	../Common/Common_Variables.txt
Resource	../Common/Keywords.txt	
Suite Setup		Open Amazon
Suite Teardown	Close Browser

*** Test Cases ***
Select Pay on Delivery Filter From Mobile Menu  
	Wait Until Element Is Visible		${MobilesButton}
	Click Element		${MobilesButton}
	Wait Until Element Is Visible		${PayOnDeliveryCheckbox}

Check All Results According to Pay on Delivery Filter	
	${valueCheckbox}=		Get Text	${PayOnDeliveryCheckbox}
	Click Element		${PayOnDeliveryCheckbox}
	Element Should Contain		${ToolbarSection}		${valueCheckbox}
	Wait Until Element Is Visible		${FilteredResults}
	${totalResults}=	Get Element Count		${FilteredResults}	
	: FOR	${singleResult}		IN RANGE		1	${totalResults}
	\	${singleResultNum}=		Convert To String		${singleResult}	
	\	${SingleFilteredResultNew}=			Replace String		${SingleFilteredResult}		NUM		${singleResultNum}
	\	Element Should Be Visible		 ${SingleFilteredResultNew}		timeout=20s
	\	${statusResults}=		Run Keyword And Return Status		Wait Until Element Is Visible		${FilteredResults}		timeout=5s
	\	${SingleFilteredResult}=	Replace String		${SingleFilteredResult}		${singleResultNum}		NUM
	Wait Until Element Is Visible		${PayOnDeliverySelectedCheckbox}
	Click Element		${PayOnDeliverySelectedCheckbox}
	Wait Until Element Is Not Visible		${ToolbarSection}
	
Select All Filters From Brands Filter Section and Check All Results According to Selected Filter
	Wait Until Element Is Visible		${BrandSection}		timeout=10s	
	${totalFilters}=	Get Element Count		${BrandSection}
	: For	${filter}		IN RANGE	1	${totalFilters}
	\	${filternum}=	Convert To String		${filter}	
	\	${num} =	Evaluate		${filternum}-1
	\	${keys}=	Get Dictionary Keys		${brandsList}		
	\	log		${keys[${num}]}
	\	log		${BrandSectionCheckbox}	
	\	${BrandSectionCheckbox}=	Replace String		${BrandSectionCheckbox}		VALUE		${keys[${num}]}
	\	log		${BrandSectionCheckbox}	
	\	Wait Until Element Is Visible		${BrandSectionCheckbox}		timeout=5s	
	\	Click Element		${BrandSectionCheckbox}
	\	${statusToolbar}=		Run Keyword And Return Status		Wait Until Element Is Visible		${ToolbarSection}		timeout=5s
	\	Run Keyword If		${statusToolbar}==False		Click Element		${BrandSectionCheckbox}
	\	Element Should Contain		${ToolbarSection}		${keys[${num}]}
	\	Wait Until Element Is Visible		${FilteredResults}
	\	${firstValue}=		Get From Dictionary		${brandsList}		${keys[${num}]}
	\	${lowerValue}=		Convert To Lowercase		${firstValue}
	\	${upperValue}=		Convert To Uppercase		${firstValue}
	\	Check Filters In Single Page		${firstValue}		${keys[${num}]}		${lowerValue}		${upperValue}
	\	${BrandSectionCheckbox}=	Replace String		${BrandSectionCheckbox}		${keys[${num}]}		VALUE	
	\	${BrandSectionSelectedCheckbox}=	Replace String		${BrandSectionSelectedCheckbox}		VALUE		${keys[${num}]}
	\	Wait Until Element Is Visible		${BrandSectionSelectedCheckbox}		timeout=5s
	\	Click Element		${BrandSectionSelectedCheckbox}
	\	${statusFilters}=		Run Keyword And Return Status		Wait Until Element Is Not Visible		${ToolbarSection}		timeout=5s
	\	${BrandSectionSelectedCheckbox}=	Replace String		${BrandSectionSelectedCheckbox}		${keys[${num}]}		VALUE		
	
	

	
	
