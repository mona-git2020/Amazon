*** Settings ***
Library		Selenium2Library	
Library		String
Library		Collections	
Resource	../Common/Common_Variables.txt
Resource	../Common/Keywords.txt	
Suite Setup		Open Amazon
Suite Teardown	Close Browser

*** Test Cases ***
Hover On Links From Mobiles 
	Wait Until Element is visible		${MobilesButton}
	Click Element		${MobilesButton}
	Wait Until Element is visible		${HoverLinkFromButton}



Verify Hover Suggestions
	${totalLinks}=		Get Element Count		${HoverLinkFromButton}
	: FOR		${link}		IN RANGE		2	${totalLinks}+2
	\	${linkNum}=		Convert To String		${link}
	\	${SingleHoverLink}=		Replace String		${SingleHoverLink}		NUM		${linkNum}
	\	${hoverLinkValue}=		Get Text		${SingleHoverLink}
	\	${hoverLinkValue}=		Remove String		${hoverLinkValue}		&
	\	${hoverLinkValue}=		Split String		${hoverLinkValue}		${SPACE}
	\	Log		${hoverLinkValue}
	\	Mouse Over		${SingleHoverLink}
	\	${hoverNum}=		Evaluate		10+${link}
	\	${hoverNum}=		Convert To String		${hoverNum}
	\	${HoverSubMenu}=		Replace String		${HoverSubMenu}		NUM		${hoverNum}
	\	Wait Until Element is visible		${ActiveLink}		timeout=25s
	\	Sleep		10s
	\	Run Keyword And Return Status		Wait Until Element is visible		${HoverSubMenu}		timeout=25s
	\	${hoverValue}=		Get Text	${HoverSubMenu}
	\	${status1}=		Run Keyword And Return Status		Element Should Contain		${HoverSubMenu}		${hoverLinkValue[0]}
	\	${status2}=		Run Keyword And Return Status		Element Should Contain		${HoverSubMenu}		${hoverLinkValue[1]}
	\	${status3}=		Run Keyword And Return Status		Element Should Contain		${HoverSubMenu}		${hoverLinkValue[2]}
	\	Run Keyword If		${status1}==False and ${status2}==False		Element Should Contain		${HoverSubMenu}		${hoverLinkValue[3]}
	\	Mouse Out		${SingleHoverLink}
	\	Sleep		10s
	\	${SingleHoverLink}=		Replace String		${SingleHoverLink}		${linkNum}		NUM	
	\	${HoverSubMenu}=		Replace String		${HoverSubMenu}		${hoverNum}		NUM		